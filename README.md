# Large Variables
- LargeInt \
    Length: 10001 \
    Range: ${-10}^{10000} ~ {10}^{10000}$ \
    Applied Calculation:
    ```
        1.Basic calculations + - * / |(as %) ^;
        2.Logic comparisons == != < > <= >=;
        3. += -= *= /= ++ -- << >> <<= >>= ~\*;
    ```
  ## Author: Hanzhe Ye
